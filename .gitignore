node_modules/
.DS_Store
*.log
.env
.env.*

# Regime library lives alongside PCC but is not part of it.
# Consumers (including PCC) should read it as an external source.
regime-library/

# Derived price history — the single largest file (~11 MB), regenerated by
# every bake and published through the Pages artifact, so it is not tracked in
# git. Committing it daily was the source of the local loose-object bloat.
# Untracked on 2026-07-11; a fresh clone must run `node build.js` to produce it
# for local serving. The live site serves the freshly-built copy, not a commit.
docs/data/history.json

# Claude Code isolation worktrees. These are local git worktrees; they must
# never be committed as gitlinks into the main tree (doing so throws a
# "No url found for submodule path" / exit 128 warning on every checkout).
.claude/worktrees/
